import type { Payment } from "@/shared/domain/payment/payment";

const registerPayment = async (fee : Payment) : Promise<Payment> => {
    throw new Error("not implemeted")
}

export default registerPayment;