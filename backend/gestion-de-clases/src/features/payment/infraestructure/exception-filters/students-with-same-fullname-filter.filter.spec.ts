import { StudentsWithSameFullnameFilter } from './students-with-same-fullname-filter.filter';

describe('StudentsWithSameFullnameFilterFilter', () => {
    it('should be defined', () => {
        expect(new StudentsWithSameFullnameFilter()).toBeDefined();
    });
});
